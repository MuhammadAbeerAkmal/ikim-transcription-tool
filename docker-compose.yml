services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env.example
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
